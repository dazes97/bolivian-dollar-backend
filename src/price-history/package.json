{
  "name": "price-history-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "offline": "sls offline start --stage dev noAuth --noTimeout --reloadHandler",
    "deploy:dev": "sls deploy --verbose --debug='*' --stage dev",
    "remove:dev": "sls remove --verbose --debug='*' --stage dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "zod": "^3.23.8",
    "mysql2": "^3.11.0",
    "async-retry": "^1.3.3",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/async-retry": "^1.4.8",
    "@types/node": "^22.1.0",
    "serverless-domain-manager": "^7.3.8",
    "serverless-esbuild": "^1.52.1",
    "serverless-offline": "^13.6.0",
    "esbuild": "^0.20.2"
  }
}
